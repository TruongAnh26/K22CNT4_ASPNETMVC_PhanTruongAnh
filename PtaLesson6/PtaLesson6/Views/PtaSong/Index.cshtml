@model IEnumerable<PtaLesson6.Models.PtaSong>

@{
    ViewBag.Title = "Danh sách bài hát";
}

<h2>Pta: Danh sách bài hát</h2>

<p>
    @Html.ActionLink("Create New", "PtaCreate")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PtaTitle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PtaAuthor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PtaArtist)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PtaYearRelease)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PtaTitle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PtaAuthor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PtaArtist)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PtaYearRelease)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
