@model IEnumerable<PtaLesson9DF.Models.Khoa>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "PtaCreate")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenKH)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenKH)
        </td>
        <td>
            @Html.ActionLink("Edit", "PtaEdit", new { id=item.MaKH }) |
            @Html.ActionLink("Details", "PtaDetails", new { id=item.MaKH }) |
            @Html.ActionLink("Delete", "PtaDelete", new { id=item.MaKH })
        </td>
    </tr>
}

</table>
